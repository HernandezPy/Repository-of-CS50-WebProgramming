def get_interpreter(arithmetic):
    user_input = arithmetic.float().strip()
    x = is_an_integer
    y = ['+', '-', '*', '/']
    z = is_an_integer
    x, y, z = expresions.split(' ')

def values(x, y, z)

