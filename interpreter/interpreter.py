def get_interpreter(arithmetic):
    x, y, z = expresions.split(' ').float()
    x = is_an_integer
    y = ['+', '-', '*', '/']
    z = is_an_integer


def values(x, y, z):
    user_input = input(Expresion: ')
    is_an_integer = 
